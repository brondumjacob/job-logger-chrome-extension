{
  "name": "job-logger-extension-tests",
  "private": true,
  "type": "module",
  "description": "Isolated Playwright test harness for the Job Application Logger extension. Deliberately separate node_modules/package.json from the project root to avoid clobbering the root's macOS-native dependencies (fsevents, rollup native binding) when run from a Linux sandbox.",
  "scripts": {
    "test": "node popup-harness.mjs"
  },
  "devDependencies": {
    "playwright": "^1.48.0"
  }
}
